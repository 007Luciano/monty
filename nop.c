#include "monty.h"
/**
 * nop - this doesn't do anything
 * @stack: the stack in the program
 * @line_number: line being read wherwe nop is encountered
 * Return: void
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
